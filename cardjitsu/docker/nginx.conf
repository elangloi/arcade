# nginx config for the Card-Jitsu game container.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Relative redirects so the game also works under a proxy path prefix
    # (e.g. /club-penguin-card-jitsu/ -> .../cardjitsu/web/).
    absolute_redirect off;
    port_in_redirect off;

    gzip on;
    gzip_types application/javascript application/json text/css image/svg+xml;
    gzip_min_length 1024;

    location = /healthz { return 200 "ok\n"; add_header Content-Type text/plain; }

    location = / { return 302 cardjitsu/web/; }

    # Art, sounds and timeline data never change between builds -> cache hard.
    location /cardjitsu/assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Game code: always revalidate so updates show up after a rebuild.
    location / {
        add_header Cache-Control "no-cache";
        try_files $uri $uri/ =404;
    }
}
