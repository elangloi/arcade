[project]
name = "sstools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lxml>=6.1.3",
    "pillow>=12.3.0",
    "pyswf",
]

[tool.uv.sources]
pyswf = { git = "https://github.com/timknip/pyswf" }
